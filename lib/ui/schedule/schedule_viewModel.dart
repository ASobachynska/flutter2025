import 'package:flutter/foundation.dart';

class ScheduleViewModel extends ChangeNotifier {
  // Клас ScheduleViewModel успадковує ChangeNotifier, що дозволяє змінювати стан і повідомляти UI
  
  //TODO: add implementation for state management on ScheduleScreen using appropriate service
  //ЗАВДАННЯ: додати впровадження для керування станом на ScheduleScreen за допомогою відповідної служби
  ScheduleViewModel();
}
